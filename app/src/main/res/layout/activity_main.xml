<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/mainText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/mainActivityText"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/mainBtn"
        android:layout_width="@dimen/mainGoBtnWidth"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/mainGoBtnMarginStart"
        android:layout_marginTop="@dimen/mainGoBtnMarginTop"
        android:layout_marginEnd="@dimen/mainGoBtnMarginEnd"
        android:layout_marginBottom="@dimen/mainGoBtnMarginBottom"
        android:background="@color/colorAccent"
        android:text="@string/mainActivityGoBtn"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/mainText" />

</android.support.constraint.ConstraintLayout>